#include <openssl/crypto.h>
#include "modes_local.h"
#include <string.h>
#if defined(STRICT_ALIGNMENT)
#else
#endif
#if defined(STRICT_ALIGNMENT)
#else
#endif
#if defined(STRICT_ALIGNMENT)
#endif
#if defined(STRICT_ALIGNMENT)
#else
#endif
