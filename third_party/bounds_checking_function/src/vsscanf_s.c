#include "secinput.h"
#if defined(SECUREC_VXWORKS_PLATFORM) && !SECUREC_IN_KERNEL && \
#include <ctype.h>
#endif
#if defined(SECUREC_VXWORKS_PLATFORM) && !SECUREC_IN_KERNEL
#endif
#if SECUREC_IN_KERNEL
#endif
