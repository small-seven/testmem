#include <common.h>
#include <asm/mmu.h>
#ifdef CONFIG_PCIE1
#endif
#ifdef CONFIG_PCIE2
#endif
#ifdef CONFIG_PCIE3
#endif
#if defined(CONFIG_PCIE1) || defined(CONFIG_PCIE2) || defined(CONFIG_PCIE3)
#endif
