#include <common.h>
#include <env.h>
#include <env_internal.h>
#if defined(CONFIG_NEEDS_MANUAL_RELOC)
#endif
#if defined(CONFIG_NEEDS_MANUAL_RELOC)
#else
#endif
#if defined(CONFIG_NEEDS_MANUAL_RELOC)
#else
#endif
