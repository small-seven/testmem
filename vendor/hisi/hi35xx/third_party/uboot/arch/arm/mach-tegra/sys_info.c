#include <common.h>
#include <linux/ctype.h>
#if defined(CONFIG_TEGRA124) || defined(CONFIG_TEGRA30)
#include <asm/arch-tegra/pmc.h>
#endif
#if defined(CONFIG_TEGRA124) || defined(CONFIG_TEGRA30)
#endif
