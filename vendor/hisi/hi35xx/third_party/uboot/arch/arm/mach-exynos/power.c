#include <common.h>
#include <asm/io.h>
#include <asm/arch/power.h>
	typedef void (*resume_func)(void);

	((resume_func)power->inform0)();
}
	typedef void (*resume_func)(void);

	((resume_func)power->inform0)();
}
