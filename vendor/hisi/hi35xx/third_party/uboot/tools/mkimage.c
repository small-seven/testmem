#include "mkimage.h"
#include "imximage.h"
#include <image.h>
#include <version.h>
#ifdef CONFIG_FIT_SIGNATURE
#else
#endif
#if defined(_POSIX_SYNCHRONIZED_IO) && \
#else
#endif
#if defined(_POSIX_SYNCHRONIZED_IO) && \
#else
#endif
