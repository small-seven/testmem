#include "boot_upg_kernel.h"
#include "boot_upg_tool.h"
#if defined(CONFIG_FLASH_ENCRYPT_SUPPORT)
#include <crypto.h>
#endif
#if defined(CONFIG_FLASH_ENCRYPT_SUPPORT)
#endif
#if defined(CONFIG_FLASH_ENCRYPT_SUPPORT)
#endif
